/** Hard cap on Tavily results ingested per competitor per sweep. */
export const MAX_RESULTS_PER_COMPETITOR = 10;

/** Max Tavily search API calls per competitor per sweep (profiles × queries). */
export const MAX_SEARCHES_PER_COMPETITOR = 3;
